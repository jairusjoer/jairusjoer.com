---

---

<span class="pagefind">
  <button
    class="pagefind-button transition-press"
    popovertarget="pagefind-popover"
    title="Search (Cmd/Ctrl + K)"
  >
    Search
  </button>

  <div
    id="pagefind-popover"
    popover
  >
    <div id="pagefind-search"></div>
  </div>
</span>

<script is:inline>
  const pagefindPopover = document.getElementById('pagefind-popover');
  let pagefindLoaded = false;

  function loadPagefind() {
    if (pagefindLoaded) return Promise.resolve();
    pagefindLoaded = true;

    const link = document.createElement('link');
    link.rel = 'stylesheet';
    link.href = '/pagefind/pagefind-ui.css';

    document.head.appendChild(link);

    return new Promise((resolve, reject) => {
      const script = document.createElement('script');
      script.src = '/pagefind/pagefind-ui.js';
      script.onload = resolve;
      script.onerror = reject;

      document.head.appendChild(script);
    });
  }

  pagefindPopover.addEventListener('toggle', async (event) => {
    if (event.newState === 'open') {
      await loadPagefind();

      if (!document.querySelector('.pagefind-ui')) {
        new PagefindUI({ element: '#pagefind-search', showSubResults: true, autofocus: true });
      }
    }
  });

  document.addEventListener('keydown', (event) => {
    if ((event.metaKey || event.ctrlKey) && event.key === 'k') {
      event.preventDefault();

      if (pagefindPopover.matches(':popover-open')) {
        pagefindPopover.hidePopover();
      } else {
        pagefindPopover.showPopover();
      }
    }
  });
</script>

<style is:global>
  :root {
    --pagefind-ui-primary: var(--color-accent) !important;
    --pagefind-ui-text: var(--color-foreground) !important;
    --pagefind-ui-background: var(--color-background-subtle) !important;
    --pagefind-ui-border: var(--color-border) !important;
    --pagefind-ui-tag: var(--color-background-subtle) !important;
    --pagefind-ui-border-width: 0.5px !important;
    --pagefind-ui-border-radius: var(--radius-default) !important;
    --pagefind-ui-image-border-radius: var(--radius-default) !important;
  }

  .pagefind-button {
    background-color: var(--color-accent);
    color: var(--color-background);
    font-weight: 600;
  }

  #pagefind-popover {
    margin: 9em auto;
    border-radius: var(--radius-default);
    background: var(--color-background);
    border: 0.5px solid var(--color-border);
    max-height: calc(100dvh - 12em);
    width: min(calc(100% - 3em), var(--spacing-default));
    padding: 1.5em;
    inset: 1.5em;
    overscroll-behavior: none;
    color: var(--color-foreground-subtle);
  }

  .pagefind-ui__result-link {
    color: var(--color-accent) !important;
  }

  /* .pagefind-ui__form {
    display: flex;
    flex-wrap: wrap;
  }

  .pagefind-ui__search-input {
    flex-grow: 1;
    margin-right: 0.75em;
  }

  .pagefind-ui__drawer {
    width: 100%;
  }

  .pagefind-ui__message {
    margin-top: 1.5em;
    font-size: smaller;
  }

  .pagefind-ui__results {
    padding-bottom: 1.5em;
    border-bottom: 0.5px solid var(--color-border);
    margin-bottom: 1.5em;
  }

  .pagefind-ui__result {
    margin-top: 1.5em;
    padding-top: 1.5em;
    border-top: 0.5px solid var(--color-border);
    display: grid;
    grid-template-columns: 1fr 5fr;
  }

  .pagefind-ui__result-title {
    font-weight: 600;
    color: var(--color-foreground);

    &:not(.pagefind-ui__result-nested *) {
      font-size: calc(1em * pow(pow(1.5, 0.2), 1));
    }
  }

  .pagefind-ui__result-excerpt {
    font-size: smaller;
  }

  .pagefind-ui__result-nested {
    padding-top: 0.75em;
    padding-left: 1.5em;
    border-left: 0.5px solid var(--color-border);
  }

  .pagefind-ui__button {
    width: 100%;
  } */
</style>
